<div class="container my-5"></div>
  <div class="row mx-5">
    <h1><%= @task.title %></h1>
    <ul>
      <li><%= @task.details %></li>
    </ul>
    <p>This task is <%= @task.completed == true ?  'completed' : 'not completed' %>.</p>
  </div>
  <div class="row m-5">
   <%= link_to 'Back to list', tasks_path %>
  </div>
</div>
